// src/features/Auth/ui/LogoutButton.jsx
import React from 'react';

const LogoutButton = ({ onClick }) => {
  return (
    <button onClick={onClick}>Logout</button>
  );
};

export default LogoutButton;
